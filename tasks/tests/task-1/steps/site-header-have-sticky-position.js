// eslint-disable-next-line @typescript-eslint/no-var-requires,no-undef
const { Given, When, Then } = require('@cucumber/cucumber');

Given(/^go to "https:\/\/www\.epam\.com"$/, () => undefined);

When('we scroll page', ()=> undefined);

Then('header always at top of viewport', () => undefined);
